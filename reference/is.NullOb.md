# A helper function that tests whether an object is either NULL \_or\_ a list of NULLs

A helper function that tests whether an object is either NULL \_or\_ a
list of NULLs

## Usage

``` r
is.NullOb(x)
```
